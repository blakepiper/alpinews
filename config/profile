# AlpineWS: Bash is the login shell. X remains an explicit startx command.
export PATH="$HOME/.local/bin:$PATH"
export SHELL=/bin/bash
export EDITOR=nvim VISUAL=nvim
# Retain ash's interactive startup path for explicitly launched ash sessions.
export ENV="${XDG_CONFIG_HOME:-$HOME/.config}/ash/rc"
